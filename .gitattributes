# Auto detect text files and perform LF normalization
* text=auto

# Ensure shell scripts use LF line endings
*.sh text eol=lf

# Docker files
Dockerfile text eol=lf
docker-compose.yml text eol=lf

# JavaScript files for WebSocket server
*.js text eol=lf
